<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ipt102project">

    <application
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IPT102Project">
        <activity android:name=".NoOrdersSelectedDialog"></activity>
        <activity android:name=".SetOrders" />
        <activity android:name=".RecyclerView_ItemView" />
        <activity android:name=".ListOrders" />
        <activity android:name=".About" />
        <activity android:name=".FoodMenu" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/button" />

        <activity android:name=".MainMenu" />
        <activity android:name=".CustomerDetails" />
        <activity android:name=".OrderDetails" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>